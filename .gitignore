# Dependencias
node_modules/
package-lock.json

# Builds / empaquetado (NO van al repo; van a GitHub Releases)
/dist/
/dist-build/
/dist-build-*/
/release/
.release-id

# Python runtime embebido (se regenera con scripts/setup-python-runtime.*) — ~80MB, no se versiona
/resources/python-runtime/
*.pyc
__pycache__/
*.exe
*.exe.blockmap
*.dmg
*.AppImage
*.deb
*.snap
*.blockmap
latest.yml
builder-debug.yml
*-unpacked/
__appImage-*/

# Logs y temporales
*.log
npm-debug.log*
*.tmp

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# SO
.DS_Store
Thumbs.db
# En Windows, `nul` es dispositivo reservado; si llega a existir como
# archivo (por un `>nul` mal redirigido en Git Bash), no entra a git.
nul

# IDE
.vscode/
.idea/
*.tsbuildinfo
